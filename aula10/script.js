// Operadores Áritméticos ( +, -, *, /, ** )
// + = Adição / Concatenação
// ** = Potenciação
// % = Resto da divisão
// Concatenação = UNIÃO DE DOIS VALORES/STRING
// NaN - Not a number // ParseInt(Números Inteiros) //parseFloat (Permite números com vírgula;)

// Precedencia da Áritmética: (), **, *, /, %, +, -; // Ordem que segue após contas matemáticas!

const num1 = 5;
const num2 = 10;
// console.log(num1 + num2);

// Operador de incremento = 'variáção'++'
// Operador de decremento = 'variáção'--'

// Operadores de atribuição
const passo = 50;
let contador = 0; 
contador += passo; // contador = contador + 50;
contador *= passo;
contador /= passo;
// contador++; // Valor do contador mais um

const number1 = 10;
const number2 = Number('5'); // parseInt // transforma uma String em números inteiros. // parseInt('5.3') - É errado! Pois só transforma em números inteiros. O correto seria trocar para ParseFloat('5.3') // Isso sim é correto por permite numeros de ponto flutuantes.

console.log(number1 * number2); // Saída NaN = Not a number





